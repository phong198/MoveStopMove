using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExpSmall : ExpPoint
{
    // Start is called before the first frame update
    private void Awake()
    {
        expID = 1;
    }
}
